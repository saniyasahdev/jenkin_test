pipeline {
    agent { label 'spark' }
    stages {
        stage('Build') {
            steps {
                echo "Testing Jenkins jobs using pipelines"
                sh "mkdir /tmp/testDir2"
                echo "Complete."
            }
        }
    }
}
